// core 안의 컴포넌트들은 ui적인 부분만을 사용해서 다양한 곳에서 내가 만들어 놓은 ui를 적용할 수 있게 만들기를 원칙으로 한다.

!! ui 이름은 생김세를 원칙으로 한다. 가장 밖의 모양,내부 모양 순으로 한다.

!! option 필수 구현 사항 
    1.크기(s,m,l,지정,full)
    2.배경 색상(primary, secondary, 지정)
    3.text 색상 (black,white,지정)
    4.기능 함수
    5.기타 내부 text 등 